import {submitAndWatch} from './avm.mjs'
import {getInterface} from './interface.mjs'

const test = async () => {

  // get a list of transactions

  // use the abi

  // sign transactions

  // send a transaction to an app
  // 
  
  submitAndWatch(signed)
	
}
